// This file is generated by the ExaHyPE toolkit.
// Please do not modify - it will be overwritten by the next
// ExaHyPE toolkit call.
// 
// ========================
//   www.exahype.eu
// ========================
// ==============================================
// Please do not change the implementations below
// ==============================================
#include "AbstractCompressibleEulerSolver_ADERDG.h"

#include "kernels/aderdg/generic/Kernels.h"

#include "kernels/GaussLegendreBasis.h"
#include "kernels/GaussLobattoBasis.h"

#include "CompressibleEulerSolver_ADERDG_Variables.h"
#include "CompressibleEulerSolver_ADERDG.h" // Have to include a proper declaration. Cannot use forward declared classes in static_cast.

#include <stdio.h>

// quadrature weights and operators
double**   CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::weights                                 = kernels::legendre::weights;
double**   CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::nodes                                   = kernels::legendre::nodes;
double***  CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::Kxi                                     = kernels::legendre::Kxi;
double***  CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::dudx                                    = kernels::legendre::dudx;
double***  CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::iK1                                     = kernels::legendre::iK1;
double***  CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::equidistantGridProjector                = kernels::legendre::equidistantGridProjector;
double***  CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::FCoeff                                  = kernels::legendre::FCoeff;
double**** CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::fineGridProjector                       = kernels::legendre::fineGridProjector;
kernels::UnivariateFunction** CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::basisFunction                 = kernels::legendre::basisFunction;
kernels::UnivariateFunction** CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::basisFunctionFirstDerivative  = kernels::legendre::basisFunctionFirstDerivative;
kernels::UnivariateFunction** CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::basisFunctionSecondDerivative = kernels::legendre::basisFunctionSecondDerivative;

// We define the constructor of the actual solver here in order to regenerate it easily.
CompressibleEuler::CompressibleEulerSolver_ADERDG::CompressibleEulerSolver_ADERDG(
  const double maximumMeshSize,
  const int maximumMeshDepth,
  const int haloCells,
  const int haloBufferCells,
  const int limiterBufferCells,
  const int regularisedFineGridLevels,
  const exahype::solvers::Solver::TimeStepping timeStepping,
  const int DMPObservables
)
  :
  AbstractCompressibleEulerSolver_ADERDG::AbstractCompressibleEulerSolver_ADERDG(
      maximumMeshSize,
      maximumMeshDepth,
      haloCells,
      haloBufferCells,
      limiterBufferCells,      
      regularisedFineGridLevels,
      timeStepping,
      DMPObservables
) {
}


CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::AbstractCompressibleEulerSolver_ADERDG(
    const double maximumMeshSize,
    const int maximumMeshDepth,
    const int haloCells,
    const int haloBufferCells,
    const int limiterBufferCells,
    const int regularisedFineGridLevels,
    const exahype::solvers::Solver::TimeStepping timeStepping,
    const int DMPObservables
)
  :
  exahype::solvers::ADERDGSolver(
      "CompressibleEulerSolver_ADERDG",
      NumberOfVariables,
      NumberOfParameters,
      NumberOfGlobalObservables,
      Order+1,
      maximumMeshSize,
      maximumMeshDepth,
      haloCells,
      haloBufferCells,
      limiterBufferCells,
      regularisedFineGridLevels,
      timeStepping,
      DMPObservables
) {
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::constantsToString(std::ostream& os) {
  // This string is used in the --version output to identify compile time constants
  os << "CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG("
     << "nVar=" << NumberOfVariables << ", "
     << "nParam=" << NumberOfParameters << ", "
     << "Order=" << Order
     << ")";
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::abortWithMsg(const char* const msg) {
  // verbosily fail even without assertions turned on
  puts(msg);
  abort();
}

int CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::fusedSpaceTimePredictorVolumeIntegral(double* lduh, double* lQhbnd, double* lGradQhbnd, double* lFhbnd, double* const luh, const tarch::la::Vector<DIMENSIONS, double>& cellCentre, const tarch::la::Vector<DIMENSIONS, double>& cellSize, const double t ,const double dt,const bool addVolumeIntegralResultToUpdate) {
  constexpr int spaceBasisSize     = (Order+1)*(Order+1);
  constexpr int spaceTimeBasisSize = spaceBasisSize*(Order+1);
  constexpr int sizeLQi   = (NumberOfVariables+NumberOfParameters)*spaceTimeBasisSize;
  constexpr int sizeRhs   = sizeLQi;
  constexpr int sizeLFi   = (DIMENSIONS + 1)*NumberOfVariables*spaceTimeBasisSize;
  constexpr int sizeGradQ = DIMENSIONS*NumberOfVariables*spaceBasisSize;
  
  constexpr int sizeLQhi = (NumberOfVariables+NumberOfParameters)*spaceBasisSize;
  constexpr int sizeLFhi = (DIMENSIONS + 1)*NumberOfVariables*spaceBasisSize;

  
  constexpr int totalSize = sizeLQi + sizeRhs + sizeLFi + sizeGradQ +
                            sizeLQhi + sizeLFhi;

  double block[totalSize];
  double* memory = block;
      
  double* lQi   = memory; memory+=sizeLQi;
  double* rhs   = memory; memory+=sizeRhs;
  double* lFi   = memory; memory+=sizeLFi;
  double* gradQ = memory; memory+=sizeGradQ;
  
  double* lQhi = memory; memory+=sizeLQhi;
  double* lFhi = memory; memory+=sizeLFhi;
  const int picardIterations = kernels::aderdg::generic::c::spaceTimePredictorNonlinear<false, true, false, false, false, CompressibleEulerSolver_ADERDG>(*static_cast<CompressibleEulerSolver_ADERDG*>(this), lQhbnd, lGradQhbnd, lFhbnd, lQi, rhs, lFi, gradQ, lQhi, lFhi, luh, cellCentre, tarch::la::invertEntries(cellSize), t, dt);
 
  if ( addVolumeIntegralResultToUpdate ) {
    kernels::aderdg::generic::c::volumeIntegralNonlinear<CompressibleEulerSolver_ADERDG, false, true, false, NumberOfVariables, Order+1>(lduh,lFhi,cellSize); 
  } else { // directly add the result to the solution
    double tempUpdate[NumberOfVariables*spaceBasisSize] = {0.0};
    kernels::aderdg::generic::c::volumeIntegralNonlinear<CompressibleEulerSolver_ADERDG, false, true, false, NumberOfVariables, Order+1>(tempUpdate,lFhi,cellSize);
    addUpdateToSolution(luh,luh,tempUpdate,dt);
  }
  

  return picardIterations;
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::addUpdateToSolution(double* const luh,const double* const luhOld,const double* const lduh,const double dt) {
  kernels::aderdg::generic::c::solutionUpdate<CompressibleEulerSolver_ADERDG>(*static_cast<CompressibleEulerSolver_ADERDG*>(this),luh,luhOld,lduh,dt);
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::faceIntegral(double* const out/*may be solution or update vector*/,double* const lFhbnd,const int direction, const int orientation,const tarch::la::Vector<DIMENSIONS-1,int>& subfaceIndex,const int levelDelta,const tarch::la::Vector<DIMENSIONS, double>& cellSize,const double dt,const bool addToUpdate) {
    
  constexpr int basisSize2 = (Order+1);
  constexpr int basisSize3 = basisSize2*(Order+1);
  constexpr int sizeUpdate = NumberOfVariables*basisSize3;

  
  if ( levelDelta > 0 ) {
    constexpr int sizeFlux = NumberOfVariables*basisSize2;
    double lFhbndCoarse[sizeFlux];
    kernels::aderdg::generic::c::faceUnknownsRestriction<CompressibleEulerSolver_ADERDG,NumberOfVariables,Order+1>(lFhbndCoarse,lFhbnd,subfaceIndex,levelDelta);
    tarch::la::Vector<DIMENSIONS,double> coarseGridCellSize = tarch::la::aPowI(levelDelta,3) * 1.0 * cellSize;
    
    kernels::aderdg::generic::c::faceIntegralNonlinear<CompressibleEulerSolver_ADERDG,NumberOfVariables,Order+1>(out,lFhbndCoarse,direction,orientation,coarseGridCellSize);
  } else {
    if ( addToUpdate ) {
      kernels::aderdg::generic::c::faceIntegralNonlinear<CompressibleEulerSolver_ADERDG,NumberOfVariables,Order+1>(out,lFhbnd,direction,orientation,cellSize);
    } else { // solution is only available in real cells, i.e. only here we can directly add the face integral result to the solution
      double lduh[sizeUpdate] = {0.0};                   // initialise with zeros
      kernels::aderdg::generic::c::faceIntegralNonlinear<CompressibleEulerSolver_ADERDG,NumberOfVariables,Order+1>(lduh,lFhbnd,direction,orientation,cellSize);
      addUpdateToSolution(out,out,lduh,dt);
    }
  }
  
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::riemannSolver(double* const FL,double* const FR,const double* const QL,const double* const QR,const double t,const double dt, const tarch::la::Vector<DIMENSIONS, double>& cellSize, const int direction, bool isBoundaryFace, int faceIndex) {
  assertion2(direction>=0,dt,direction);
  assertion2(direction<DIMENSIONS,dt,direction);
kernels::aderdg::generic::c::riemannSolverNonlinear<false,false,CompressibleEulerSolver_ADERDG>(*static_cast<CompressibleEulerSolver_ADERDG*>(this),FL,FR,QL,QR,t,dt,cellSize,direction);
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::boundaryConditions(double* const fluxIn,const double* const stateIn,const double* const gradStateIn, const double* const luh,const tarch::la::Vector<DIMENSIONS,double>& cellCentre,const tarch::la::Vector<DIMENSIONS,double>& cellSize,const double t,const double dt,const int direction,const int orientation) {
  constexpr int basisSize     = (Order+1);
  constexpr int sizeStateOut = (NumberOfVariables+NumberOfParameters)*basisSize;
  constexpr int sizeFluxOut  = NumberOfVariables*basisSize;
  
  constexpr int totalSize = sizeStateOut + sizeFluxOut;
  double block[totalSize];
  double* memory = block;

  double* stateOut = memory; memory+=sizeStateOut;
  double* fluxOut  = memory; memory+=sizeFluxOut;

  const int faceIndex = 2*direction+orientation;
    kernels::aderdg::generic::c::boundaryConditions<false,CompressibleEulerSolver_ADERDG>(*static_cast<CompressibleEulerSolver_ADERDG*>(this),fluxOut,stateOut,fluxIn,stateIn,gradStateIn,cellCentre,cellSize,t,dt,faceIndex,direction);
  if ( orientation==0 ) {
    double* FL = fluxOut; const double* const QL = stateOut;
    double* FR = fluxIn;  const double* const QR = stateIn;
  
    riemannSolver(FL,FR,QL,QR,t,dt,cellSize,direction,true,faceIndex);
  }
  else {
    double* FL = fluxIn;  const double* const QL = stateIn;
    double* FR = fluxOut; const double* const QR = stateOut;

    riemannSolver(FL,FR,QL,QR,t,dt,cellSize,direction,true,faceIndex);
  }
}

double CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::stableTimeStepSize(const double* const luh,const tarch::la::Vector<DIMENSIONS,double>& cellSize) {
  double d = kernels::aderdg::generic::c::stableTimeStepSize<CompressibleEulerSolver_ADERDG,false>(*static_cast<CompressibleEulerSolver_ADERDG*>(this),luh,cellSize);
  return d;
}
void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::adjustSolution(double* const luh,const tarch::la::Vector<DIMENSIONS,double>& cellCentre,const tarch::la::Vector<DIMENSIONS,double>& cellSize,double t,double dt) {
  kernels::aderdg::generic::c::solutionAdjustment<CompressibleEulerSolver_ADERDG>(*static_cast<CompressibleEulerSolver_ADERDG*>(this),luh,cellCentre,cellSize,t,dt);
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::faceUnknownsProlongation(double* const lQhbndFine,double* const lFhbndFine,const double* const lQhbndCoarse,const double* const lFhbndCoarse,const int coarseGridLevel,const int fineGridLevel,const tarch::la::Vector<DIMENSIONS-1,int>& subfaceIndex) {
  kernels::aderdg::generic::c::faceUnknownsProlongation<CompressibleEulerSolver_ADERDG,NumberOfVariables,NumberOfParameters,Order+1>(lQhbndFine,lFhbndFine,lQhbndCoarse,lFhbndCoarse,coarseGridLevel,fineGridLevel,subfaceIndex);
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::volumeUnknownsProlongation(double* const luhFine,const double* const luhCoarse,const int coarseGridLevel,const int fineGridLevel,const tarch::la::Vector<DIMENSIONS,int>& subcellIndex) {
  kernels::aderdg::generic::c::volumeUnknownsProlongation<CompressibleEulerSolver_ADERDG,NumberOfVariables,NumberOfParameters,Order+1>(luhFine,luhCoarse,coarseGridLevel,fineGridLevel,subcellIndex);
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::volumeUnknownsRestriction(double* const luhCoarse,const double* const luhFine,const int coarseGridLevel,const int fineGridLevel,const tarch::la::Vector<DIMENSIONS,int>& subcellIndex) {
  kernels::aderdg::generic::c::volumeUnknownsRestriction<CompressibleEulerSolver_ADERDG,NumberOfVariables,NumberOfParameters,Order+1>(luhCoarse,luhFine,coarseGridLevel,fineGridLevel,subcellIndex);
}

//--------


void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::resetGlobalObservables(double* const globalObservables) {
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::updateGlobalObservables(
    double* const                               globalObservables,
    const double* const                         luh,
    const tarch::la::Vector<DIMENSIONS,double>& cellCentre,
    const tarch::la::Vector<DIMENSIONS,double>& cellSize,
    const double t,
    const double dt) {
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::mergeGlobalObservables(double* const globalObservables,const double* const otherObservables) {
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::wrapUpGlobalObservables(double* const globalObservables) {
}

//--------

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::flux(const double* const Q,double** const F) {
      abortWithMsg("flux: If this operation is entered, you have activated the corresponding guard. Then you have to re-implement this routine, too." );
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::viscousFlux(const double* const Q,const double* const gradQ, double** const F) {
      abortWithMsg("viscous flux: If this operation is entered, you have activated the corresponding guard. Then you have to re-implement this routine, too." );
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::viscousEigenvalues(const double* const Q,const int direction,double* const lambda) {
      abortWithMsg("viscous eigenvalues: If this operation is entered, you have activated the corresponding guard. Then you have to re-implement this routine, too." );
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::algebraicSource(const tarch::la::Vector<DIMENSIONS, double>& x, double t, const double *const Q, double *S) {
      abortWithMsg("algebraicSource: If this operation is entered, you have activated the corresponding guard. Then you have to re-implement this routine, too." );
}

    void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::nonConservativeProduct(const double* const Q,const double* const gradQ,double* const BgradQ) {
      abortWithMsg("nonConservativeProduct: If this operation is entered, you have activated the corresponding guard. Then you have to re-implement this routine, too." );
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::multiplyMaterialParameterMatrix(const double* const Q, double** const rhs) {
      abortWithMsg("multiplyMaterialParameterMatrix: If this operation is entered, you have activated the corresponding guard. Then you have to re-implement this routine, too." );
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::pointSource(const double* const Q,const double* const x,const double t,const double dt, double* const forceVector,int n) {
      abortWithMsg("pointSource: If this operation is entered, you have activated the corresponding guard. Then you have to re-implement this routine, too." );
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::resetGlobalObservables(GlobalObservables& globalObservables) const {
  abortWithMsg("If this operation is entered (resetGlobalObservables), you have specified global observables. Then you have to re-implement this routine, too." );
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::mapGlobalObservables(
    GlobalObservables&                          globalObservables,
    const double* const                         luh,
    const tarch::la::Vector<DIMENSIONS,double>& cellCentre,
    const tarch::la::Vector<DIMENSIONS,double>& cellSize,
    const double t,
    const double dt) const {
  abortWithMsg("If this operation is entered (mapGlobalObservables), you have specified global observables. Then you have to re-implement this routine, too." );
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::mergeGlobalObservables(
    GlobalObservables&         globalObservables,
    ReadOnlyGlobalObservables& otherObservables) const {
  abortWithMsg("If this operation is entered (mergeGlobalObservables), you have specified global observables. Then you have to re-implement this routine, too." );
}

void CompressibleEuler::AbstractCompressibleEulerSolver_ADERDG::wrapUpGlobalObservables(GlobalObservables& globalObservables) const {
  // Please re-implement if required.
}


/**
 * Fallback implementations of joined functions. Users can either safely ignore this
 * or overwrite with their own implementations.
 **/
#include "kernels/fusedMethods.cpph"